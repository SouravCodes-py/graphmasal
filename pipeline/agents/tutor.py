from state import StudentState

def run_tutor(state: StudentState) -> dict:
    # STUB — replace with real LLM call + streaming on Day 4-5
    return {
        "tutor_response": "The root issue is Chain Rule. Let's work through it step by step.",
        "quiz_triggered": False,
        "new_concept_flagged": False,
    }
