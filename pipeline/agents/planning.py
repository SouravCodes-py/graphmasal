from state import StudentState

def run_planning(state: StudentState) -> dict:
    # STUB — replace with MSMS / BFS on Day 4-5
    return {
        "learning_path": ["Chain Rule", "Composite Functions", "u-substitution", "Integration"],
        "path_rationale": "Chain Rule is the weakest prerequisite on the path to Integration.",
        "estimated_concepts": 3,
    }
