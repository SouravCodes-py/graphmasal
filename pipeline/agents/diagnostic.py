from state import StudentState

def run_diagnostic(state: StudentState) -> dict:
    # STUB — replace with real Cypher traversal on Day 2-3
    return {
        "root_cause": "Chain Rule",
        "root_mastery": 0.34,
        "diagnosis_depth": 2,
    }
